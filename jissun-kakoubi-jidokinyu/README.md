# 「加工日」図面自動記入プロジェクト 構想サマリー

> **注記**：本プロジェクトの本体はこのリポジトリの他フォルダ（GAS × GitHub Pages構成のWeb
> アプリ群）とは異なり、**ユーザーのローカルWindows PC上で動作するPythonアプリ（実寸法師の
> UI自動操作、pywinauto使用）**です。ソースコード本体・exe配布物は社内サーバー
> （`\\192.168.3.2\share_01\⑰その他\Claude(sumi)\実寸法師で製品情報テキスト入力\`）上で管理
> されており、このリポジトリには含まれません。このREADME.mdは、ローカルのClaude Codeセッ
> ションとクラウド版Claude Codeセッションの間で引き継ぎを行うための**構想・実装状況の記録
> （ドキュメントのみ）**として保存しています。2026-09-08時点でユーザーがローカル実機での検証
> を一定量完了させた段階のスナップショットです。
>
> **（2026-09-09追加）**このフォルダには上記ドキュメントに加えて、社内の「アプリランチャー」
> （`app-launcher/`）から本プロジェクトの2つのローカルファイルへワンクリックで到達するための
> **軽量なGAS × GitHub Pagesミニアプリ**（`index.html`・`style.css`・`app.js`・`gas/Code.gs`）
> も同居させている。詳細は12章参照。

Claude Code（クラウド版）での検討結果をまとめたもの。ローカルのClaude Codeセッションで実装・実機検証を継続するための引き継ぎ資料。

**2026-09-08時点の状況（GUIアプリ化・exe配布まで完了）**：Python実装一式に加えて**GUIアプリ本体`app.py`と、それをPyInstallerで固めた単体exe（`実寸法師アプリ.exe`、Python不要）を作成し、実機で「exeをダブルクリック→Excel選択→列選択→製品テーブルから対象を選んで実行」という一連の流れが動くところまで到達**（9〜11章参照）。当初検討していた「20件ずつの自動バッチ分割」は不採用となり、**製品を一覧テーブルから複数選択して実行する方式**に置き換わっている。差分検出も「建方日・加工日の2列だけを比較」から「実際に組み立てるテキスト内容(選択した列すべて)が完全一致するか」を比較する方式に強化済み（11章）。まだExcel全行（267図番）を対象にした本実行はしていない。

**別プロジェクトとの関係**：「実寸法師を開くアプリ」（マスタExcel横断検索→CAD起動Webアプリ、`jissun://`独自プロトコル使用、このリポジトリの`open_jissun/`に該当）は本プロジェクトとは**別物**。ただし同じExcelマスタ（建方日・加工日・図番ハイパーリンク列）を扱うため、そちらで判明したデータの表記ゆれ（7.1・7.2参照）はこちらにも取り込んだ。詳細はメモリ`project_jissunho_open_app`参照。

### 次回セッションでまず読む場所（引き継ぎサマリー）

- **今すぐ動くもの**：プロジェクトルート直下の`実寸法師アプリ.exe`をダブルクリックするだけで起動する（Pythonインストール不要、全社員が同じexeをサーバー上で直接実行できる）。開発・不具合調査用にPython版`app.py`（GUI）・`main.py`（CLI、`python main.py "<Excelパス>" --only "図番1,図番2"`）も残置（9章参照）
- **できていること**：Excel読み込み・列選択・差分検出(テキスト完全一致判定)・製品テーブルからの複数選択・実寸法師のUI自動操作(保存後に自動で閉じる)・検証フォルダへの保存・完了後にエクスプローラーで検証フォルダを開く、まで一通り実装済み。GUIの一連の流れ（Excel選択→事前設定確認→列選択→製品テーブル選択→実行→進捗画面→自動終了→検証フォルダを開く）を**ユーザーが実機で一定量検証済み。問題となる箇所は見つかっていない**（2026-09-08時点）
- **次にやるべきこと（優先順）**：
  1. 中規模件数（10〜20図番程度）で試験実行してから、Excel全行（267図番）の本実行に進む
  2. 全件本実行の際は、保存確認ダイアログの出現タイミングが環境依存で揺れないか（7.4章参照）や、大量件数を製品テーブルに表示した際の描画速度も併せて様子見する
  3. 実行状態フォルダは2026-09-08にリセット済み（既存の状態JSON・検証フォルダの出力はユーザー判断で削除済み）。直近の検証で新しい状態JSONが作られ始めている状態
- **絶対に守ること**：フェーズ1の間は**検証フォルダ以外への保存は行わない**（原本の直接上書きは禁止、4.3章のフェーズ2はまだ未実装）

---

## 1. 背景・目的

Excelマスターデータ（シート名：`ﾏｽﾀｰﾃﾞｰﾀ`）で鉄骨製品を管理している。

- **F列（図番）**：各行の図面番号。セルにハイパーリンクが設定されており、リンク先は実寸法師（鉄骨CADアプリ）の図面データファイル（`.tdf`）。UNCパス例：
  `file://192.168.1.2/share/①進行中工事/26-16 カゴヤ多々羅キャンパス.../WA1-0C-01　260619.tdf`
- **G列（製品マーク）**：図面内の個別製品の識別名（例：`0C-X17Y5`）
- **X列（加工）**：加工日。日付が入っていれば加工済み、`*` なら未加工

**やりたいこと**：X列に加工日が入力された製品について、F列のハイパーリンク先である実寸法師の図面ファイルを開き、その図面上に「加工日」情報を書き込んで保存する作業を自動化したい。

### データの重要な特性（実データで確認済み）

- 全114行に対し、F列（図番）のユニーク値は44種類 → **1つの.tdfファイルに複数の製品マーク（行）が紐づくケースが大半**（例：`WA1-1B-02`は12製品分）
- つまり1図面＝複数製品＝複数の異なる加工日があり得る
- **【2026-09-08訂正】**上記「114行・44図番」は当時ローカルにあった一部抜粋コピーの統計だった。**社内サーバーの実マスタ全体を対象にした実際の値は820行・267図番**（うち製品マーク空欄・ハイパーリンク無しの行を除外済み、詳細は8章参照）。想定より処理規模が大きいので留意すること

---

## 2. 実寸法師について

- ローカルWindowsアプリ（ユーザーのPCにインストール済み）。マクロ・API・コマンドライン自動化機能は**無い**（通常のUI操作でのみ図面編集可能）
- 図面ファイルは独自バイナリ形式（拡張子 `.tdf`）

---

## 3. 検討した2つのアプローチ

### アプローチA：.tdfバイナリを直接書き換える（× 不採用）

実際に実験を行い、以下が判明した。

**分かったこと（構造解析メモ、参考情報）**
- ファイル先頭に `TDF` マジックバイト
- テキストラベルのレコード形式を発見：
  ```
  [pre_field: 4byte] [X座標: double] [Y座標: double] [高さ: double] [幅: double] [回転角: double]
  [pad: 4byte=0] [id_field: 4byte(自己参照)] [marker: "00 08 10 00"]
  [val1: 4byte = テキストバッファ長+8] [val2: 4byte(用途不明の参照値)]
  [テキストバッファ: null終端+4バイト境界パディング]
  [トレーリング16バイト(フラグ類)] [次レコードへのポインタ的な値]
  ```
- 文字コードはCP932（Shift-JIS）
- ファイル末尾に `AC0`〜`AC255` という固定256件の管理テーブル（色コード等）を発見
- ヘッダー部（オフセット0x90/0x94）に、この末尾テーブルの先頭アドレスを指すポインタを発見（＝ファイルは複数の独立したリスト構造を持ち、ヘッダーの先頭/末尾ポインタで管理されている）

**実験結果**
- 上記構造を模して新規テキストレコードをファイル末尾に追記 → **実寸法師でエラーなく開けたが、追記した文字は表示されなかった**
- 原因：新規レコードがどの描画対象リストにも接続されていないため。レコード同士のリンク（`pre_field`／`val2`等）の正確な意味・正しい繋ぎ方が、1ファイルの解析だけでは特定できなかった
- これ以上の追い込みは「一見動いても他のデータを壊すリスク」があり見合わないと判断 → **このアプローチは断念**

### アプローチB：UI自動操作（○ 採用）

実寸法師の画面スクリーンショットで実際の操作を確認済み。

**確認できた操作フロー（1製品あたり）**
1. 実寸法師で対象 `.tdf` ファイルを開く
2. 右パネルで「文字」→「通常」を選択（機能0の一覧から。機能1=通常/枠内/バルーン/連結/引出/一覧、機能2=通常/引出/2点、機能3=通常/複数行）
3. 画面下部の文字列入力ボックスに文字列（例：`0C-X17Y5_8/27加工済`）を入力してOK（Enterキーでも右の「OK」ボタンクリックでも同じ動作）
4. 「表示位置を指定してください」の入力欄に**座標値を直接キーボード入力できる**（例：`0,0` → Enter）。マウスクリックでのピクセル位置特定は不要 ← **これが自動化の最大の難所を解消する重要発見**
5. 保存（上書き保存 or 名前を付けて保存）

**メニュー構造（ファイルメニュー、Altニーモニック対応）**
```
ファイル(F)
├ 新規作成(N)
├ テンプレートで新規作成(L)...
├ 開く(O)...
├ 閉じる(C)
├ 最近使ったファイル(D)
├ 上書き保存(S)
├ 名前を付けて保存(A)...
├ ファイル名変更(M)...
├ 読み込み(R)... / 書き出し(W)... / 追加読み込み(G)... / 抽出書き出し(T)... / 図面枠読み込み(F)...
├ 印刷(P)... / 部分印刷(E)...
├ 送信(D)...
├ 実寸法師の設定(I)...
├ 実寸法師の一時終了(U)
└ 実寸法師の終了(X)  Alt+F4
```
→ Alt+F,O（開く）、Alt+F,S（上書き保存）、Alt+F,A（名前を付けて保存）等、**キーボード操作（メニューニーモニック）で駆動できる可能性が高い**

**左パネルのレイヤー一覧**（実データのバイナリ内にも同名の文字列を確認済み）：
`2ND_DEK, BASE, BEAM_S, COMMON, FRAME, GEN, JOINT, JOINT_SPL, KEYPLAN, KEY_PLAN, LIST, LIST_1, MAIN_POST, PCORE, POST, RIB_PL, SECTION, SUB_BEAM, 免震装置`

---

## 4. 確定した設計方針（2026-09-04 実機検証を経て更新）

| 項目 | 決定内容 |
|---|---|
| 実現方式 | UI自動操作（実寸法師を実際に操作。バイナリ直接編集は不採用。実機検証で確立済み→[[project_kakoubi_jidokinyu_app]]） |
| 操作手段 | ツール選択はメニューニーモニック（`Alt+C`→`1`→`t`→`n`→`n`）。文字列入力・座標入力はUIA直接操作＋キー入力。マウスのピクセル座標依存はほぼ排除できた |
| **書き込み内容（2026-09-08 再変更・列選択式に）** | 固定フォーマットをやめ、**書き込み前の列選択画面（ボタンUI）**で含める列をトグルON/OFFして決定する方式に変更。詳細・具体例は4.2参照 |
| 配置位置 | 図面座標 **(0, 0)** に固定（1行目）。カンマ区切り入力は無効なので「X」「Y」のラベルを個別クリックして数値入力する |
| 複数製品対応（確定・採用） | 「複数行」モードは不採用（挙動不安定）。**「通常」モードを製品数ぶん繰り返し、Y座標を -(サイズ+100)×index ずつオフセットして積み重ねる**方式を正式採用。各行は独立したテキストオブジェクトになる |
| 更新方式 | 実行のたびに実寸法師で**原本を新規に開き直し**、その図番に属する**全製品分をゼロから配置**して別名保存する（原本は常に無編集のまま開くだけなので「既存テキストの削除」は不要になった） |
| **実行形態（2026-09-08 実装完了、9章参照）** | GUIアプリ`app.py`をPyInstallerで単体exe化した`実寸法師アプリ.exe`（Python本体・ライブラリ同梱、対象PCにPythonのインストール不要）を作成済み。**全社員がサーバー上の同じexeを直接ダブルクリックするだけで実行できる**（実機で警告なく起動することを確認済み）。開発・不具合調査用のPython版（`app.py`/`main.py`）も残置。退社前に手動実行→PCは起動したまま帰宅、という運用は変わらず。pywinautoのSendInputによるPC操作占有は解消されない点に注意（9章参照） |
| 実行主体 | 基本的に本人（1人）が1台のPCから実行想定 |
| **対象Excel（2026-09-08 サーバー移行に伴い更新）** | ローカルコピーではなく**社内サーバーの`マスタ全自動`フォルダ内の実マスタExcelを直接読み込む**（`\\192.168.3.2\share_01\⑰その他\Claude(sumi)\マスタ全自動\`配下の`○○＿マスタのまま.xlsx`群、工事ごとに1ファイル）。GUIでは同フォルダ内のファイルを自動列挙して一覧から選ぶ方式（9章参照）。①過去に実行歴がある（＝対応する状態JSONが存在する）ファイルなら前回の記録と比較、②初めて指定するファイルなら状態JSONを新規作成する。これにより1本のアプリで工事（Excelマスタ）ごとに使い回せる |
| **処理範囲（2026-09-08 再変更、11章参照）** | 「前回実行日時」との比較は当初から廃止済み。さらに2026-09-08、**行単位の比較基準を「建方日・加工日の2列」から「実際に組み立てるテキスト内容(選択した列すべて)」に変更**。行ID（A列）単位で前回出力した**テキスト文字列そのもの**を状態JSONに記録し、現在の内容と完全一致するかを突き合わせる。一致しない（未記録＝新規行を含む）行があれば、その行が属する**図番全体**を再処理対象にする。ただし実際にJissunへ配置する対象は、GUIの製品テーブルでユーザーが選択した行だけに絞れる（9章参照） |
| 安全策（フェーズ1・現在ここ） | **検証フォルダに全図番の結果をまとめて新規保存**。原本（ローカルコピーでもネットワーク共有上の原本でも）は絶対に上書きしない。実機で複数回確認済み |
| 安全策（フェーズ2・将来） | 問題なければ本体（共有フォルダの原本）へ直接保存。更新の都度、自動でバックアップを作成。**具体的な更新方式は4.3章参照（2026-09-08、当初案から修正）** |
| エラー処理 | バッチ処理中に1件（1図番）失敗しても、その図番だけスキップして次へ進み、最後に失敗一覧を表示。失敗した図番は状態JSONを更新しないため次回自動的に再処理対象になる |

### 4.1 フォルダ構成（2026-09-08 社内サーバーへ移行・確定）

プロジェクト一式をローカル（`C:\Users\sumi\Desktop\Claudeローカルセッション\実寸法師に加工日を入れる\`）から社内サーバーへ移行した。

| フォルダ | パス | 用途 |
|---|---|---|
| プロジェクトルート | `\\192.168.3.2\share_01\⑰その他\Claude(sumi)\実寸法師で製品情報テキスト入力\` | 構想サマリー（本ファイル）・今後作成するPythonスクリプト一式を格納 |
| 対象Excel群 | `\\192.168.3.2\share_01\⑰その他\Claude(sumi)\マスタ全自動\` | 工事ごとの実マスタExcel（`○○＿マスタのまま.xlsx`）。本プロジェクトの読み取り専用データソース。書き込みはしない |
| 検証フォルダ | プロジェクトルート`\検証\` | フェーズ1安全策の出力先（実寸法師で書き込み後の.tdfを保存）。旧ローカル`検証`フォルダの中身をここへ移動済み |
| 実行状態フォルダ | プロジェクトルート`\実行状態\` | Excelファイルごとの状態JSON（4章「状態管理の設計変更」参照）を保存 |

- ローカルの旧作業フォルダには、サーバーへのショートカット（`実寸法師で製品情報テキスト入力.lnk`）とテスト用に取得していたExcelコピー（`カゴヤ多々羅_マスタのまま.xlsx`、現在は未使用）のみ残置。今後の読み取りは上記マスタ全自動フォルダの実マスタが正となる
- 複数人が同じサーバーフォルダにアクセスしうるため、実行スクリプトは**実行状態フォルダ・検証フォルダへの書き込み時の競合（同時実行）**を将来的に考慮する必要がある（現状は「基本的に本人1人が実行」想定のため未対応、TODO）

### 4.2 列選択画面（ボタンUI）の仕様（確定・2026-09-08）

固定フォーマット（製品マーク＋建方日＋加工日のみ）をやめ、**どの列を書き込み内容に含めるかをユーザーがボタンで選べる**方式に変更した。

| 項目 | 決定内容 |
|---|---|
| 表示タイミング | **（2026-09-08変更）Excelファイル選択の直後、差分検出より前に1回だけ**表示するよう変更。差分検出（どのテキストが変わったか）の判定自体に「今回どの列を使うか」の情報が必要になったため、列選択→差分検出→製品テーブル、の順にした（11章参照。旧仕様は「処理対象を洗い出した直後」だったが、これは差分検出が建方日・加工日の2列固定だった頃の名残） |
| デフォルトON | **建方（B列）・ブロック（C列）・長さ（K列）・重量（M列）・加工（X列）** の5列。ボタンで解除（OFF）可能 |
| 追加可能 | 上記5列以外の列（6章の全列構成）は初期OFFで、リストから選んでONに追加できる |
| 製品マーク（G列） | ボタンの選択対象には**含めない**。常に固定で各行テキストの先頭に入る |
| 列の並び順 | 選択された列は元Excelの列順（B→C→…→X→…）に準拠して連結する |
| 1行のテキスト形式 | `製品マーク＿ラベル：値＿ラベル：値…` の「ラベル：値」形式で＿（アンダーバー）連結。ラベル名は列見出し（建方日・ブロック・長さ・重量・加工日 等）を使う |
| 組み立て例 | 建方=9/4、ブロック=A、長さ=3200、重量=123.4、加工=8/27 を選択した場合：<br>`0C-X17Y5＿建方日：9/4＿ブロック：A＿長さ：3200＿重量：123.4＿加工日：8/27` |
| 日付列の整形 | 建方日・加工日は従来通り7.1の`format_date_or_mitei`でM/D or 未定に整形。他の日付列（塗装日・出荷等）を追加した場合も同関数を適用 |
| 想定実装 | Tkinterのボタン（トグル）画面を想定（実行主体は1人・退社前手動実行のためGUI表示前提でよい） |
| 複数製品への適用範囲 | 選択した列構成は**その回の実行全体（全対象図番・全製品）に一律適用**する（図番や製品ごとに異なる列構成を選ぶ機能は現状想定しない） |

**確定（2026-09-08 実装セッションで決定）**：
- 非日付列（ブロック・長さ・重量など）が空欄の場合も、建方日・加工日と同じ「未定」と表示する
- 選択列構成（今回どの列を選んだか）は状態JSON側にも`selected_columns`として記録し、次回同じExcelファイルで起動した際のデフォルト選択として復元する

### 4.3 フェーズ2（原本直接更新時）の設計メモ（2026-09-08・未実装・将来検討のみ）

**今回の実装スコープには含めない**。フェーズ1（検証フォルダへの新規保存のみ、4章「安全策」参照）の範囲でまず実装・検証し、将来フェーズ2（原本への直接保存）に進む際の設計方針としてここに記録しておく。

**課題**：フェーズ1は「原本は毎回無編集のまま新規に開くだけ」という前提のおかげで、既存テキストの削除ロジックが不要だった（5章-3、7.3章参照）。しかしフェーズ2で原本に直接書き込むようになると、この前提が崩れる。

- 原本の図面はCAD上で**人による通常の図面修正（追加変更）も継続的に発生する**。当初案「初回書き込み時の原本をまっさらバックアップとして保存しておき、再処理のたびにそのバックアップから作り直して原本に上書きする」は、**バックアップ以降に人が加えた正当な図面修正を巻き戻してしまう**ため不採用（2026-09-08、実装セッションでユーザー指摘により却下）
- 採用する考え方：
  1. 「記録済みか否か」の判定は、CAD側を直接読み返すのではなく、**このアプリが別途持つ記録（状態JSON、[[state_manager.py]]相当）**を見て行う。現在のExcel値（建方日・加工日）が記録済みの値と一致していれば「スルー」（原本を開かない）。この判定ロジック自体はフェーズ1の`state_manager.find_dirty_drawings`とほぼ同じものがそのまま使える
  2. 記録済みだが値が変わっており更新が必要な場合は、**原本を「ゼロから全行配置し直す」のではなく、実寸法師上で該当テキストオブジェクトを直接編集する**（削除＋再配置ではなく、既存テキストの内容だけを書き換える）。これにより、原本内の他の（人による）図面修正箇所には触れずに済む
- 上記2.を実現するには、「このアプリが過去にどの座標・どの内容でテキストを配置したか」を状態JSON側に記録しておき、実寸法師上でその座標のテキストオブジェクトを選択→編集する自動化ロジック（現状のフェーズ1にはまだ無い、新規検討事項）が必要になる
- 安全策としての「更新の都度、自動でバックアップを作成」（4章参照）は、上記のような「復元用バックアップ」ではなく、**監査証跡・万一のロールバック用のタイムスタンプ付きバックアップ**という位置づけで、そのまま維持する

### 状態管理の設計変更（2026-09-04）：「前回実行日時」比較→行単位JSON比較

**問題点**：当初案「前回実行日時より新しい加工日（X列）を持つ図番だけ処理」には見落としがあった。X列（加工日）は「実際に加工した日付」であり「Excelに入力した日時」ではないため、後日まとめて過去日付を入力する運用（バックフィル）があると、単純な日付比較では見逃してしまう（例：前回実行日=9/1、後日9/5に8/20分をまとめて入力→8/20<9/1なので「古い」と誤判定されスキップされてしまう）。

**採用する方式**：Excelファイルごとに状態JSONを持ち、**行ID（A列）単位で前回出力した値を記録**して現在値と突き合わせる。

- 状態JSONの保存場所：サーバーの`実行状態\`フォルダ（`\\192.168.3.2\share_01\⑰その他\Claude(sumi)\実寸法師で製品情報テキスト入力\実行状態\`、4.1章参照）配下に、Excelファイル名（拡張子なし）をキーにしたファイル名で保存（例：`カゴヤ多々羅_マスタのまま.json`）
- 状態JSONの構造イメージ：
  ```json
  {
    "excel_path": "（フルパス、参照用）",
    "rows": {
      "2616Y-1-": {"図番": "WA1-0C-01", "建方日": "9/4", "加工日": "8/27"},
      "...": {}
    }
  }
  ```
- 実行フロー：
  1. 指定されたExcelファイルに対応する状態JSONを探す。**あれば読み込む（①既存ファイルとの比較）／なければ空の状態として扱う（②新規作成）**
  2. Excel全行を読み、行ID（A列）ごとに現在の（建方日, 加工日）と状態JSONの記録を比較
  3. 記録と異なる（未記録＝新規行を含む）行があれば、その行が属する**図番を再処理対象**としてマーク
  4. 再処理対象の図番について、実寸法師で原本を開き直し、その図番に属する**全行**（未定含む）をゼロから配置して検証フォルダへ保存
  5. 保存成功後、その図番に属する全行の記録を状態JSONに書き込む（保存）。失敗した図番は記録を更新しない（次回また対象になる）
- メリット：「前回実行日時」という曖昧な概念が不要になり、バックフィル入力にも正しく対応でき、Excelファイルを変えても（＝別の工事でも）同じ仕組みがそのまま使い回せる

### バックグラウンド実行についての制約（2026-09-04検討）

- pywinautoの`click_input`/`type_keys`は実際のマウス・キーボード入力（`SendInput`）を使うため、**実行中はPCの操作を奪ってしまう**（ユーザーが同時に別作業できない）
- Anthropic公式のClaude「コンピュータ操作のバックグラウンド実行」機能は2026-09-04時点で**macOS限定・Windows未対応**のため使えない
- 対応案を検討した結果、**「最後にPCを離れる人が退社前に実行し、PCは点けたまま帰宅する」**運用で解決することにした（技術的な変更は不要、運用でカバー）
- 将来的な技術的対応案（保留）：①メッセージベース操作（PostMessage/UIA Invoke）に書き換えて別の仮想デスクトップで動かす（実寸法師の独自描画コントロールが反応するか未検証）、②リモートデスクトップ等の別セッションで完全分離

---

## 5. 実装前に検証・確定すべき項目（更新：2026-09-04実機検証で解決済みの項目に✅）

1. ✅ **「文字」ツールの起動方法**：メニューニーモニック（`Alt+C`→`1`→`t`→`n`→`n`）で座標非依存に選択可能と判明
2. ✅ **座標入力の正確な書式**：カンマ区切りは無効。「X」ラベルクリック→数値入力→「Y」ラベルクリック→数値入力（マイナス値も可）→Enter→Enterで確定
3. ✅ **既存テキストの削除方法**：原本は毎回無編集のまま新規に開くだけなので不要と判明（削除ロジック自体を実装しなくてよい）
4. ✅ **ファイルを開く操作**：Excelハイパーリンク先のUNCパスを`urllib.parse`で復号し、`subprocess.Popen([Jissun.exeパス, パス])`で起動可能。ただし**実寸法師はシングルインスタンス**で、2つ目以降は既存プロセスにファイルが転送される点に注意
5. ✅ **保存時の確認ダイアログ**：2026-09-08実機確認済み。ボタン名「はい(&Y)」を自動クリックする処理を実装・動作確認済み（詳細は7.3・7.4章参照）
6. ✅ **自動化ツールの選定**：Python + pywinauto（backend="uia"）で確立
7. ✅ **Excelデータの読み取り方法**：openpyxlで確立。B列（建方日）・G列（製品マーク）・X列（加工/加工日）を読み取り、F列のハイパーリンクからUNCパスを取得

---

## 6. 参考：元Excelファイルの列構成（`ﾏｽﾀｰﾃﾞｰﾀ`シート）

```
A:ID  B:建方日  C:ブロック  D:部位  E:加工先  F:図番(ハイパーリンク)  G:製品マーク
H:サイズ  I:備考① J:備考② K:長さ L:本数 M:重量 N:塗装 O:形状 P:SPL・方杖
S:コア1 T:コア2 U:パネル1 V:パネル2 W:大組
X:加工(加工日)  Y:溶接 Z:UT AA:UT合否 AB:外観合否 AC:寸法検査 AD:合否 AE:受入
AF:塗装日 AG:横持 AH:出荷 AI:出図
```

---

## 7. 実機検証で確立したpywinauto実装知見（2026-09-04策定、2026-09-08コード化・実機テスト完了）

以下の知見はすべて`jissun_automation.py`として実装済み、実機3図番のバッチ実行で動作確認済み（8章参照）。

### 7.1 テキスト生成ロジック（確定・2026-09-08 姉妹プロジェクトの知見を反映し更新）

姉妹プロジェクト「実寸法師を開くアプリ」（同じExcelマスタの建方日・加工日列を扱う）の実装で、**日付セルは実日付／未入力（Excelのシリアル値0付近。openpyxlでは年≤1900のdatetimeや`datetime.time(0,0)`として読めることがある）／文字列（`*`等）の3パターンが実データに混在する**ことが判明済み。当初想定していた「None or `*` 文字列」の2パターンだけでは不十分なため、下記の通り防御的に修正する。

```python
import datetime

def format_date_or_mitei(value):
    if isinstance(value, datetime.datetime):
        if value.year <= 1900:
            return "未定"  # Excelのシリアル値0付近＝未入力扱い
        return f"{value.month}/{value.day}"
    if isinstance(value, datetime.time):
        return "未定"  # ごく稀にtime型として読まれるケースあり
    return "未定"  # None・空欄・"*" 等の文字列はすべて未定扱い

def build_text(mark, tatekata, kakou):
    return f"{mark}＿建方日：{format_date_or_mitei(tatekata)}＿加工日：{format_date_or_mitei(kakou)}"
```
実データでテスト済み（例：`0C-X17Y5＿建方日：9/4＿加工日：8/27`、`1G-Y5X17＿建方日：未定＿加工日：未定`）。年≤1900・time型の分岐は姉妹プロジェクトの知見からの取り込みで、本プロジェクトではまだ実データでの遭遇は未確認（念のための防御コード）。

**（2026-09-08実装・確定）**：4.2章の設計変更に沿って、上記は「建方日・加工日固定」ではなく**選択された列を可変で連結する**形に一般化して実装済み（`text_builder.py`）。

```python
NUMERIC_UNIT_SUFFIX = {"長さ": "m", "重量": "t"}  # 浮動小数点ノイズ除去＋単位付与

def format_value_or_mitei(key, value):
    if key in DATE_COLUMNS:
        return format_date_or_mitei(value)
    if value is None:
        return "未定"
    if key in NUMERIC_UNIT_SUFFIX and isinstance(value, (int, float)):
        rounded = round(float(value), 2)
        num_text = str(int(rounded)) if rounded == int(rounded) else f"{rounded:.2f}"
        return f"{num_text}{NUMERIC_UNIT_SUFFIX[key]}"
    text = str(value).strip()
    if not text or text == "*":
        return "未定"
    if isinstance(value, float) and value == int(value):
        return str(int(value))
    return text

def build_line_text(product_mark, values, selected_columns):
    """1製品ぶんのテキストを組み立てる:「製品マーク＿ラベル：値＿ラベル：値…」。"""
    parts = [product_mark]
    for key in selected_columns:
        parts.append(f"{key}：{format_value_or_mitei(key, values.get(key))}")
    return "＿".join(parts)
```
非日付列（ブロック・長さ・重量等）が空欄の場合も「未定」表示、長さ・重量は小数第2位丸め＋単位付与で確定（4.2章「確定（2026-09-08 実装セッションで決定）」参照）。この`build_line_text`の出力そのものが、11章で説明する「差分検出（テキスト完全一致判定）」の比較対象になる。

### 7.2 ハイパーリンク→UNCパス変換（2026-09-08 姉妹プロジェクトの知見を反映し更新）

姉妹プロジェクトの実装で、**xlsxハイパーリンクの表記ゆれが実データに混在する**ことが判明済み：①`file://host/share/...`（スラッシュ区切り、これまで本プロジェクトで遭遇していたパターン）、②`file:///\\host\share\...`（バックスラッシュ混じり）の2パターン。本プロジェクトでは①しか実機確認していないが、同じExcelマスタ由来のため②も混在している可能性があり、両対応にしておく。

```python
from urllib.parse import unquote, urlparse

def hyperlink_to_unc(url):
    if url is None:
        return None
    s = unquote(url)
    if "\\\\" in s:
        # パターン②: file:///\\host\share\... のようにバックスラッシュがそのまま埋め込まれている
        return s[s.find("\\\\"):]
    # パターン①: file://host/share/...%20.../WA1-0C-01　260619.tdf
    parsed = urlparse(url)
    return "\\\\" + unquote(parsed.netloc + parsed.path).replace("/", "\\")
```

### 7.3 実寸法師操作フロー（1図番あたり、2026-09-08 実機テストで確定・コード化済み）

1. `subprocess.Popen([Jissun.exeパス, UNCパス])`でファイルを開く（**シングルインスタンス**：2回目以降は既存プロセスに転送され新規プロセスはすぐ終了する）。`Application(backend="uia").connect(path=Jissun.exeパス, ...)`をタイトルにファイル名が含まれるまでポーリングして接続する方式で確立（`jissun_automation.open_drawing`）
2. `main_win.maximize()` → `set_focus()`（最大化必須、座標系が安定する）
3. `main_win.type_keys("%c", pause=0.15)` → `"1"` → `"t"` → `"n"` → `"n"`（各キーの間に0.3秒程度sleep）で「文字/通常/通常」選択
4. その図番に属する全行をループ：
   - 下部のEditコントロール（`main_win.descendants(control_type="Edit")[0]`）に`set_edit_text(text)`（UIA上`window_text()`では読み取れないが実際の入力・見た目には正しく反映される。読み取り検証は不可、実機スクリーンショットで確認するしかない）
   - `window_text()=="OK"`のPane要素をクリック
   - 「X」ラベル（auto_id=88）クリック→座標入力、「Y」ラベル（auto_id=89）クリック→オフセット値（1行目=`0`、2行目以降は`-(サイズ+100)×index`の負数）入力
   - Enter→Enter（2回目で確定配置）
5. 「表示(V)」→「全体(F)」は`main_win.menu_select("表示(V)->全体(F)")`で実行可能（実機確認済み、標準メニューのため座標非依存）。監査目的のスクリーンショットは任意
6. 「名前を付けて保存」は`main_win.type_keys("%f")`→`"a"`でダイアログを開く。ダイアログ検索は**実寸法師のプロセスに限定**して行う（`pywinauto.findwindows.find_windows(class_name="#32770", process=app.process)`）。ファイル名欄・保存ボタンは`dialog.descendants(class_name="Edit"/"Button")`で取得し、**検証フォルダへのフルパス**を`set_edit_text`→保存ボタンクリック
7. 上書き確認ダイアログが出た場合は、ボタン名「はい(&Y)」を`descendants(class_name="Button")`から`"はい" in b.window_text()`で検索してクリック（実機確認済み、7.4章参照）
8. **（2026-09-08追加・ユーザー要望）保存完了後、実寸法師を閉じる**：`main_win.type_keys("%{F4}")`（Alt+F4）で終了。保存直後のため通常は確認ダイアログは出ないはずだが、万一「変更を保存しますか」的なダイアログが出た場合は二重保存を避けるため「いいえ」を優先してクリックする（`jissun_automation.close_jissun`）。この処理自体の失敗は致命的ではない（保存は既に完了している）ため、例外は握りつぶして呼び出し側に伝播させない設計にしている

**win32バックエンド特有の重要な注意点（2026-09-08実機で判明）**：
- `main_win`（Jissun本体、UIAバックエンド）の要素検索は`control_type=`でよいが、**保存ダイアログ等の標準Windowsダイアログ（win32バックエンド）は`class_name=`で絞り込む**必要がある。`control_type=`を使うと該当0件になり見つからない
- ダイアログをクラス名だけで探す場合、**システム全体（`win32gui.FindWindow`）ではなく対象プロセスに限定**（`find_windows(..., process=app.process)`）しないと、無関係な別アプリの同クラス名ウィンドウを誤って掴む危険がある

### 7.4 未検証・要注意事項（2026-09-08更新：主要項目は実機検証済みに）

- ✅ **保存先に同名ファイルが既に存在する場合の上書き確認ダイアログ**：実機確認済み。ボタン名「はい(&Y)」「いいえ(&N)」、メッセージは「〜は既に存在します。上書きしますか?」。自動クリック実装・動作確認済み（`jissun_automation.save_as`）
- ✅ 行ID単位の状態JSON（`実行状態\{Excelファイル名}.json`）の読み書きロジック：`state_manager.py`として実装・テスト済み（4章参照）
- 複数行オフセット `-(サイズ+100)` は条件パネルの実際のフォントサイズを自動化側で読み取れない（カスタム描画で値取得不可）ため、**サイズが変わっても固定オフセット値を使う想定**（サイズ200のときに実機確認した値）。将来的にズレが気になる場合は要調整
- ✅ 実機バッチ処理：2026-09-08、カゴヤ多々羅マスタから3図番（単一製品×2、4製品×1）を選んで`main.py`で実行し、全件成功・原本は無編集のまま保護されていることを確認済み（8章参照）。**Excel全行（267図番）を対象にした本実行はまだ行っていない**
- 状態JSONのファイル名をExcelファイル名（拡張子なし）だけで決めると、同名だが別フォルダの異なるExcelファイルが将来出てきた場合に衝突する可能性がある（現状は運用上ほぼありえない想定だが、気になる場合はフルパスのハッシュ値をファイル名に含める対応も検討可）
- 7.1の「年≤1900・time型」防御、7.2の「ハイパーリンク表記ゆれ②」対応は、姉妹プロジェクト「実寸法師を開くアプリ」での実データ知見からの取り込みであり、**本プロジェクト（カゴヤ多々羅マスタ）ではまだ実際にそのパターンに遭遇していない**（念のための防御コード）。将来別のExcelマスタを対象にする際は特に注意
- ✅ 列選択画面（4.2章）：`column_selector.py`としてTkinterで実装・動作確認済み（8章参照）。ただし**まだmain.pyには組み込んでいない**（現状main.pyは常に前回選択 or デフォルト5列を使用。列選択UIの呼び出しは次回以降の課題）
- ✅ サーバー移行（4.1章）に伴うパス変更：`excel_reader.py`・`state_manager.py`・`main.py`とも実装済みのパスで実機確認済み
- 未検証：保存時の確認ダイアログが**環境やタイミングによって出現が遅れる／出現しないケース**があるかもしれない（今回は全て安定して即座に出現したが、ネットワーク共有越しのファイルI/O速度に左右される可能性がある。本実行前にもう少し実行回数を重ねて様子を見るのが望ましい）

---

## 8. 実装状況（2026-09-08、GUIアプリ・exe化まで完了）

構想・設計段階から実装段階に進み、プロジェクトルート（4.1章）配下に以下のファイルを作成した。9〜11章でこの後の設計変更（GUIアプリ化・製品テーブル選択・テキスト完全一致判定・実寸法師の自動終了等）を詳述する。

| ファイル | 役割 | 状態 |
|---|---|---|
| `excel_reader.py` | Excelマスタの見出しベース列特定・行抽出、ハイパーリンク→UNC変換 | 実データ検証済み（820行/267図番を正しく抽出、製品マーク空欄・ハイパーリンク無し行は除外） |
| `state_manager.py` | 状態JSON読み書き、差分検出（11章の通りテキスト完全一致ベースに変更済み） | 単体テスト済み（完全一致→スキップ／列変化→検知／列選択変化→検知／図番単位グルーピング維持、の4パターン確認） |
| `text_builder.py` | 日付整形・数値列の丸め・単位付与、選択列を可変連結する1行テキスト組み立て（7.1章） | 実データで動作確認済み |
| `column_selector.py` | 列選択画面（Tkinter）。デフォルト5列・追加選択・前回選択の復元に対応 | 実装・動作確認済み。9章の通り**差分検出より前**に呼ばれるよう順序変更済み |
| `jissun_automation.py` | pywinautoによる実寸法師のUI自動操作（開く・文字配置・保存・上書き確認・**保存後に閉じる**） | 実機で段階的に検証済み |
| `runner.py` | GUI/CLI共有のバッチ処理ロジック（`process_drawing`・`run_drawings`、進捗コールバック対応） | 9章参照 |
| `app.py` | **GUI本体（Tkinter）**。9〜10章参照 | 実機で画面遷移・製品テーブル選択→実行の一連の流れが動作することを確認済み |
| `main.py` | CLI版（開発・不具合調査用、`python main.py <Excelパス> [--only 図番...] [--columns ...]`） | `runner.py`を使うようリファクタ済み |
| `実寸法師アプリ.exe` | `app.py`をPyInstallerで固めた単体exe（10章参照） | 実機でサーバー上から直接起動できることを確認済み |
| `実寸法師インストール確認.bat` | 実寸法師が既定パスにあるか自己診断し、無ければ入力したパスを`%APPDATA%`配下にPCごとに保存するツール（10章参照） | 「見つかった」分岐は実機確認済み |

### 8.1 実機バッチテスト結果（2026-09-08、CLI版`main.py`で実施）

カゴヤ多々羅マスタから以下3図番を選び、`main.py --only "WB2-0C-01,WB2-0C-02,WA1-1B-05"`で実行。

- WB2-0C-01（1製品）、WB2-0C-02（1製品）、WA1-1B-05（4製品）の**全3件が成功**
- 複数製品図番（WA1-1B-05）は4製品分のテキストが正しく積み重ねて配置され、重複や欠落なく検証フォルダへ保存されたことをスクリーンショットで確認
- **原本4ファイルの更新日時を実行前後で比較し、一切変更されていないことを確認済み**（安全策どおり検証フォルダにのみ新規保存）
- 実行後、検証フォルダに3件の新規ファイルが作成され、ファイルサイズも配置した行数に応じて適切に増加していることを確認

**注（2026-09-08）**：上記テストで生成された状態JSON（カゴヤ多々羅6行/3図番）と検証フォルダの出力は、11章の差分検出方式変更にあわせてユーザー判断で削除済み。実行状態フォルダ・検証フォルダは現在空の状態からの再スタートになる。

### 8.2 次回以降の課題（2026-09-08、GUIの実機検証完了を踏まえて更新）

- **GUIアプリの一連の流れ（Excel選択→列選択→製品テーブル選択→実行→実寸法師の自動終了→検証フォルダを開く）はユーザーが実機で一定量検証済み。問題となる箇所は見つかっていない**（2026-09-08）
- Excel全行（267図番）を対象にした本実行はまだ実施していない。まずは中規模件数（10〜20図番程度）で様子を見てから全件実行に進むのが望ましい
- 保存確認ダイアログの出現タイミングが環境依存で揺れないか、実行回数を重ねて様子見（7.4章参照）
- フェーズ2（原本直接更新、4.3章）は今回のスコープ外のまま
- 実寸法師インストール確認.batの「見つからない→入力→PCごとの設定ファイル書き込み」分岐は、開発環境固有の制約により自動テストができていない。実際にパスが異なるPCに遭遇した際に実機確認が必要（10.3章参照）

---

## 9. GUIアプリ化（2026-09-08、ユーザー要望「アプリ化していきたい」に対応）

CLI（`main.py`）に加えて、Tkinter製のGUIアプリ`app.py`を新規作成した。CLIと処理内容を共有するため`runner.py`にバッチ処理ループを切り出し（`process_drawing`・`run_drawings`）、進捗をコールバックで通知できるようにしている（GUIのTextウィジェットへ流し込むため）。

### 9.1 画面遷移（確定・実装済み）

1. **Excelファイル選択**：`マスタ全自動`フォルダ内の`*マスタのまま*.xlsx`を自動列挙し、リストから選ぶ（4.1章の対象Excel参照）
2. **事前設定確認ポップアップ**：「実寸法師は最後に設定されたテキスト条件で入力されます／マスタ情報を入力する『テキストサイズ、レイヤ、色』を事前に設定して実寸法師を閉じてください」と表示し、「設定済み」ボタンで次へ（9.2章「レイヤ・色の指定方式」参照）。**（2026-09-08追加）**見落とし防止のため「実寸法師を設定して閉じてから実行」を赤文字・2倍サイズ・0.7秒間隔でゆっくり点滅させて表示
3. **列選択画面**：既存の`column_selector.py`をそのまま再利用。**（2026-09-08変更）差分検出より前に呼ばれる**（11章の通り、差分検出自体に列選択の情報が必要になったため）
4. **製品テーブルからの選択**：差分検出（11章）で「変更あり」と判定された全製品を一覧テーブル（`ttk.Treeview`）に表示し、実行する製品を複数選択（デフォルト全選択、Ctrl/Shiftクリックで複数選択、「全選択」「全解除」ボタンあり）。**1件だけの実行も可能**（9.3章参照）
5. **実行・進捗ログ画面**：バックグラウンドスレッドで`runner.run_drawings`を実行し、`queue`経由でログをTextウィジェットへ反映。完了後、実寸法師を閉じ（9.4章）、検証フォルダをエクスプローラーで開く（9.5章）

### 9.2 レイヤ・色の指定方式（2026-09-08、要望と不採用案）

ユーザーから「テキストサイズ・レイヤ・色を指定したい」と要望があった。自動化コード側（`jissun_automation.py`）で左パネルのレイヤ一覧や条件パネルの色ピッカーをpywinautoで直接操作する案は、正確な操作手順が未検証で難易度が高いため**不採用**。代わりに、**実行前のポップアップでユーザー自身が実寸法師側の「最後に使った設定」を希望の状態にしてから閉じてもらう**運用にした（9.1章の手順2）。実寸法師の「文字/通常」ツールは最後に使った設定（サイズ・レイヤ・色）をアプリ内に保持しているため、自動化コードを一切変更せずに実現できる。詳細はメモリ`feedback_jissun_format_not_important`にも追記済み。

### 9.3 実行対象の選択方式（2026-09-08、20件自動分割案からの変更）

当初「処理対象が20件（図番）を超えたら、全件実行/20件ずつ分割実行を選ばせる」という案を検討したが、ユーザーから「20件自動選択の仕組みは削除し、製品名が並ぶテーブルから複数選択して実行する仕組みが欲しい（1件だけの実行もできるように）」との要望があり、**この案は不採用・削除**。代わりに9.1章の手順4の製品テーブル選択方式を採用した。

- テーブルの1行＝1製品（Row）。列は「図番・製品マーク・建方日・加工日」
- **図番単位のグルーピングは維持**：ユーザーが1図番の中の一部の製品だけを選んでも、その図番を開く際は選択された製品だけを配置する。ただし差分検出（11章）で「変更あり」と判定された図番は、その図番に属する全行がテーブルに現れる（変更が無い製品まで含めて選択できる設計ではなく、そもそも「変更が無い＝テーブルに出てこない」ため実務上は問題にならない）
- **重要な正確性の修正**：この方式に対応するため、`state_manager.record_success()`を「図番全体をsheetから再導出して記録」から「実際に処理した行（rows）だけを記録」する方式に変更した。旧実装のままだと、一部の製品だけを選んで実行した場合に、実際には配置していない未選択の製品まで「記録済み」にしてしまうバグがあった

### 9.4 保存後に実寸法師を閉じる（2026-09-08追加・ユーザー要望）

1図番の処理が完了する（保存が終わる）たびに、実寸法師を自動で終了するようにした（`jissun_automation.close_jissun`、7.3章の手順8参照）。Alt+F4で終了し、万一確認ダイアログが出た場合は二重保存を避けるため「いいえ」を優先してクリックする。次の図番の処理では、シングルインスタンスの制約通り新しいプロセスが起動する（またはファイルが転送される）。**閉じる処理自体の失敗は致命的ではない**設計（保存は既に完了しているため、例外を握りつぶして処理全体の失敗にはしない）。

### 9.5 完了後に検証フォルダを開く（2026-09-08追加・ユーザー要望）

バッチ全体（選択した全製品の処理）が完了した時点で、検証フォルダ（`os.startfile(VERIFY_DIR)`）を1回だけエクスプローラーで開くようにした。図番ごとに開くと件数が多い場合にウィンドウが大量に開いて煩雑になるため、意図的に「バッチ全体の完了時に1回だけ」としている。

---

## 10. 実行形態・Python不要配布（2026-09-08）

### 10.1 PyInstallerによるexe化

「他のユーザーのPCでも使えるようにしたい」という要望を受け、`app.py`をPyInstallerで単体exe化した。

- ビルド方法：ソース一式をローカルフォルダ（UNCパスではなく）にコピーしてから`python -m PyInstaller --onefile --name "実寸法師アプリ" app.py`を実行し、生成された`dist\実寸法師アプリ.exe`をサーバーへコピーする運用
- 成果物：`実寸法師アプリ.exe`（約36MB、Python本体・openpyxl・pywinauto・tkinter等を全同梱）。プロジェクトルートに配置
- **実機確認済み**：サーバー上のexeを直接ダブルクリックし、警告なく起動することを確認済み。全社員がサーバーへの閲覧アクセス権を持っているため、追加の配布作業は不要
- 現状`--windowed`は付けずビルドしている（エラー発生時にコンソールでトレースバックが見えるようにするため意図的）。将来的に安定したら`--windowed`化を検討
- **重要な運用ルール**：`app.py`等のソースを修正した場合、`実寸法師アプリ.exe`は自動更新されない。修正のたびに再ビルド→サーバーへ再配置が必要
- 開発・不具合調査用にPython版（`app.py`/`main.py`）も残置。Python環境がある開発機向けの起動用`.bat`（`実寸法師アプリを起動.bat`）は一時期用意したが、exe版があれば不要と判断し2026-09-08に削除済み

### 10.2 実寸法師のインストールパス確認・PCごとの上書き設定

他PCで実寸法師のインストールパスが既定（`C:\Program Files (x86)\Taiwa\Jissun\jissun.exe`）と異なる場合に備え、以下を整備した。

- `実寸法師インストール確認.bat`：既定パスの有無をダブルクリックで自己診断できるツール。見つからない場合は実際のパスの入力を促し、入力・存在確認ができれば自動でPCごとの設定ファイルに書き込む
- `jissun_automation.py`に`resolve_jissun_exe()`を追加：`%APPDATA%\実寸法師アプリ\jissun_path.txt`（そのPC・そのWindowsユーザー専用）があればそちらを優先、無ければ従来通り既定パスを使う。**共有ソース/exeを直接書き換えると全員に影響するため、PCごとのローカル設定として分離**した。この設定ファイルが存在しない限り、既存ユーザーの挙動は一切変わらない（実機で確認済み）

### 10.3 未検証事項

「見つからない→パス入力→設定ファイル書き込み」の分岐は、開発環境固有の制約（Bash/PowerShellツールから起動したPythonプロセスの`%APPDATA%`が実際のファイルシステムと一致しない現象）により自動E2Eテストができなかった。ロジック自体は単純なファイル存在チェック+フォールバックのみで手動レビュー済みだが、実際に既定パスと異なるPCに遭遇した際に実機での動作確認が必要。

---

## 11. 差分検出の再設計：「テキスト完全一致」判定への変更（2026-09-08）

### 11.1 きっかけ

ユーザーから「一度実行したものについて、テキスト抽出内容（条件も含む）が完全一致の場合は『新しく開いて保存する必要が無い』ので無視したい。何か記録は取ってる？」と質問があった。確認したところ、**当時の実装は建方日・加工日の2列しか記録・比較していなかった**（状態管理の設計変更、174行目付近の当初設計のまま）。これには以下の抜け穴があった。

1. 建方日・加工日以外の選択列（ブロック・長さ・重量等）が変わっても検知できない
2. 列選択（条件）自体を変えても検知できない（同じ建方日・加工日なら、列構成を変えても再処理されない）

### 11.2 変更内容

- `state_manager.find_dirty_drawings(sheet, state, selected_columns)`：シグネチャに`selected_columns`を追加。行ごとに`build_line_text()`で実際に配置するテキストを組み立て、状態JSONに記録された`text`と完全一致するかで dirty 判定する（以前は`建方`/`加工`の2フィールドだけを比較していた）
- `state_manager.record_success()`：記録内容を`{"建方":.., "加工":..}`から`{"text": build_line_textの出力}`に変更
- 状態JSONの構造（新）：
  ```json
  {
    "excel_path": "（フルパス、参照用）",
    "selected_columns": ["建方", "ブロック", "長さ", "重量", "加工"],
    "rows": {
      "2616Y-1-": {"図番": "WA1-0C-01", "text": "0C-X17Y5＿建方：9/4＿ブロック：A＿..."},
      "...": {}
    }
  }
  ```
- **図番単位のグルーピングは維持**：1図番の中の一部の行だけが変化した場合でも、その図番に属する全行が再処理対象になる（従来通り）。これは「原本を毎回ゼロから開き直して配置」という設計上、変化した行だけを配置すると未変化の行の情報が出力ファイルから欠落してしまうため、意図的に維持している
- **列選択→差分検出の順序変更**：差分検出に列選択の内容が必要になったため、`app.py`・`main.py`とも「列選択」を「差分検出」より前に行う順序に変更した（9.1章参照）

### 11.3 効果

- ①建方日・加工日以外の選択列が変わった場合も正しく検知できるようになった（以前の抜け穴を修正）
- ②列選択（条件）自体を変えた場合も正しく検知できるようになった（以前の抜け穴を修正）
- ③内容が完全一致なら図番ごと再処理をスキップできるようになった（ユーザー要望どおり）

ロジックは単体テストで検証済み（完全一致→スキップ、非建方/加工列の変化→検知、列選択変更のみ→検知、図番単位グルーピング維持、の4パターンで確認）。

### 11.4 既存データの移行

既存の状態JSON（カゴヤ多々羅6行/3図番、堺データセンター153MW（後期）50行/20図番）は、上記変更を適用すると次回実行時に「変更あり」として再検出される（古い記録に新しい比較用の`text`項目が無いため）。実害はない（検証フォルダへの再保存だけで原本には触れない）が、ユーザー判断で**移行せず両ファイルとも削除**し、実行状態フォルダを空の状態からリスタートすることにした（併せて検証フォルダの出力も削除済み）。

---

## 12. アプリランチャー連携（2026-09-09追加）

社内の「アプリ一覧」ランチャー（`app-launcher/`、Googleスプレッドシート「アプリ一覧Launcher」
の「情報」シート駆動）から、本プロジェクトの以下2つのローカルファイルへワンクリックで
到達したいという要望があった。

- ①`実寸法師インストール確認.bat`（実寸法師の実行パス確認）
- ②`実寸法師アプリ.exe`（実寸へマスタ情報を自動追加するGUI本体）

### 12.1 検討した方式

| 方式 | 内容 | 採否 |
|---|---|---|
| `file://` UNCリンクをapp-launcherの「情報」シートに直接2行追加 | コード変更は不要だが、`.exe`/`.bat`のような実行可能ファイルは多くのブラウザ（Edge/Chrome）がfile://リンククリックで直接実行せず「ダウンロード」に倒す可能性が高い（`.tdf`のような通常のデータファイルとは扱いが異なる） | 保留（未検証のまま採用せず） |
| `jissun://`独自プロトコルのURLをapp-launcherに直接2行追加 | 姉妹プロジェクト「実寸法師を開くアプリ」（`製品名より実寸を開く`フォルダ）で確立済みの、対象PCに登録済みのプロトコルハンドラーを使う方式。ユーザーが既にこの方式で**ローカル.batファイルの起動に成功したことを確認済み** | 採用（ただし①②を直接ランチャーの行にするのではなく、下記12.2の専用ミニアプリ経由にする） |

ユーザーの意向により、app-launcherの「情報」シートに①②を直接2行追加するのではなく、
**①②へ飛ぶための専用の小さなアプリ（本フォルダ）を新規に作り、ランチャーにはそのアプリの
GitHub PagesのURLを1件だけ登録する**方式を採用した。

### 12.2 実装（本フォルダに同居）

他のGAS × GitHub Pagesアプリ（`open_jissun/`・`production-management/`等）と同じ構成に
揃えた、番号付きボタン2つだけの単純なランチャーページ。

```
jissun-kakoubi-jidokinyu/
  README.md    … 本ファイル(構想サマリー + 本セクション)
  index.html   … フロントエンドのHTML(画面構造のみ)
  style.css    … スタイル(ダーク基調、①②の番号付きボタン)
  app.js       … 「情報」シートから項目を取得し、番号付き<a href>ボタンとして描画
                 + クリックログ送信(GAS APIとの通信、text/plainでPOST)
  gas/Code.gs  … GAS側バックエンド(「情報」シート読み取り専用のJSON API)
```

- ボタンは実際の`<a href="jissun://...">`タグとして実装している（JSの`location.href`代入は
  ブラウザにブロックされることがあるため、ブラウザ標準のリンク遷移に任せる方が確実、という
  姉妹プロジェクト`open_jissun`での知見を踏襲）
- 表示する項目（①②）は「情報」シートに行として持たせる方式（コード変更なしで項目の増減・
  文言修正が可能。他アプリと同じ設計思想）

### 12.3 セットアップ手順（ユーザー側で実施が必要）

1. 新規のGoogleスプレッドシートを作成し（名称は任意、例:「実寸法師マスタ入力 起動ランチャー」）、
   「情報」「記録」の2シートを用意する
2. 「情報」シート（1行目はヘッダー: `ID / 名前 / URL / 説明`）の2行目以降に、以下の2行を入力する
   （URLは11章時点で確定した`jissun://`形式のリンク。実機で①②それぞれ単体の動作確認が
   まだの場合は、先にそちらを済ませてから登録することを推奨）：

   | ID | 名前 | URL | 説明 |
   |---|---|---|---|
   | 1 | 実寸法師 実行パス確認 | `jissun://192.168.3.2/share_01/%E2%91%B0%E3%81%9D%E3%81%AE%E4%BB%96/Claude%28sumi%29/%E5%AE%9F%E5%AF%B8%E6%B3%95%E5%B8%AB%E3%81%A7%E8%A3%BD%E5%93%81%E6%83%85%E5%A0%B1%E3%83%86%E3%82%AD%E3%82%B9%E3%83%88%E5%85%A5%E5%8A%9B/%E5%AE%9F%E5%AF%B8%E6%B3%95%E5%B8%AB%E3%82%A4%E3%83%B3%E3%82%B9%E3%83%88%E3%83%BC%E3%83%AB%E7%A2%BA%E8%AA%8D.bat` | 実寸法師の実行パスが正しく設定されているか確認します |
   | 2 | 実寸法師アプリ起動 | `jissun://192.168.3.2/share_01/%E2%91%B0%E3%81%9D%E3%81%AE%E4%BB%96/Claude%28sumi%29/%E5%AE%9F%E5%AF%B8%E6%B3%95%E5%B8%AB%E3%81%A7%E8%A3%BD%E5%93%81%E6%83%85%E5%A0%B1%E3%83%86%E3%82%AD%E3%82%B9%E3%83%88%E5%85%A5%E5%8A%9B/%E5%AE%9F%E5%AF%B8%E6%B3%95%E5%B8%AB%E3%82%A2%E3%83%97%E3%83%AA.exe` | 実寸へマスタ情報を自動追加します(①を先に実行しておくこと) |

   このURLはユーザーが提示した`jissun://`URL実例（同じフォルダ内の別ファイル向け）と
   同じ規則（UTF-8バイト単位のパーセントエンコード、`(`/`)`もエンコード）で機械的に再現した
   ものであり、実際のプロトコルハンドラー側の実装は未確認である旨は本人にも伝達済み。
3. スプレッドシート上部の「拡張機能」→「Apps Script」を開き、`gas/Code.gs`の内容をまるごと
   貼り付けて保存する
4. 「デプロイ」→「新しいデプロイ」→種類「ウェブアプリ」、次のユーザーとして実行:「自分」／
   アクセスできるユーザー:「全員」で発行する
5. 発行されたウェブアプリのURL（`https://script.google.com/macros/s/xxxxx/exec`）を
   Claudeとのチャットに貼り付ける（`app.js`先頭の`GAS_API_URL`に反映してGitHubへpushする。
   現時点では`PASTE_NEW_DEPLOYMENT_URL_HERE`のプレースホルダーのまま未設定）
6. `main`ブランチにマージ済みであれば、`https://aism24.github.io/-/jissun-kakoubi-jidokinyu/`
   で公開される
7. 社内の「アプリ一覧Launcher」スプレッドシートの「情報」シートに、このURLを1行追加する
   （アプリ名は任意、例:「実寸法師 マスタ情報入力」。分類は既存の実寸法師関連の分類に揃える）

### 12.4 未検証事項（2026-09-09時点）

- ①②それぞれのjissun://URLが実機で正しく起動するかは未確認（11章の注意点と同じ）
- GASのデプロイURL未設定のため、現状`app.js`はプレースホルダーのままでは動作しない
  （手順3〜5の完了が必須）
- ①（インストール確認）→②（アプリ起動）の順で使うことを想定した文言・番号付けにしている
  （4.1章の運用フロー「①実寸法師実行パス確認→②実寸へマスタ情報追加作業」に対応）
